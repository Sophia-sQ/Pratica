#include <bits/stdc++.h>
#include <iostream>
#include <vector>
using namespace std;

/*int main(int argc, char const *argv[])
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N, D, A, btn=0;
    cin>>N>>D>>A;
    while (A!=D)
    {
        A++; btn++;
        if (A>N)
        {
            A=1;
        }


    }

    cout<<btn<<"\n";
    return 0;
}
*/

/*int main(int argc, char const *argv[])
{
    int N;
    int botasD[61]={0};
    int botasE[61]={0};
    char lado;
    int num;
    int pares = 0;
    cin >> N;
    for (int i = 0; i < N; i++)
    {
        cin >> num >> lado;
        if (lado == 'E'){
            botasE[num]++;
        } else{
            botasD[num]++;
        }

        if (botasD[num]>0 && botasE[num]>0){
            botasD[num]--;
            botasE[num]--;
            pares++;
        }
    }

    cout << pares<<"\n";
    return 0;
}*/

int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}

