#include <stdio.h>
#include <math.h>
int main(int argc, char const *argv[])
{
    int bolas[250], i, n, cont=0, soma=0, grupos=0;
    double pont;

    scanf("%d", n);
    grupos=n;
    pont=(pow(4, n));
    for (i = 0; i < pont ; i++)
    {
        
        if(soma>n) continue;
        if (soma==n) cont++;
        
    }
    

    return 0;
}
